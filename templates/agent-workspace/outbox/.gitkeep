# Mailbox placeholder

This directory is an agent's **outbox**: messages this agent wants the
*operator* to see. See [`reference/mailboxes.md`](../../../reference/mailboxes.md).

Files are not tracked in this repo — each installed agent's mailbox is its own.
This `.gitkeep` only preserves the directory shape for the template.
